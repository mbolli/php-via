{% extends 'examples/_wrapper.html.twig' %}

{% block demo %}
{# via:cut #}
<div class="flex flex-col gap-2">
    {{ counter1|raw }}
    {{ counter2|raw }}
    {{ counter3|raw }}
</div>
{# via:cut #}
{% endblock %}
